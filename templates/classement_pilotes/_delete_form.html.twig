<form method="post" action="{{ path('app_classement_pilotes_delete', {'classementPId': classement_pilote.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classement_pilote.id) }}">
    <button class="btn">Delete</button>
</form>
