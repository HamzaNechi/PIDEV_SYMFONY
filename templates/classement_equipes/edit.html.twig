{% extends 'base-back.html.twig' %}

{% block title %}Edit ClassementEquipes{% endblock %}

{% block body %}
<div class="page-body">
    <h1>Edit ClassementEquipes</h1>

    {{ include('classement_equipes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_classement_equipes_index') }}">back to list</a>

    {{ include('classement_equipes/_delete_form.html.twig') }}
     </d>
{% endblock %}
