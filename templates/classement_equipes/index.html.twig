{% extends 'base-back.html.twig' %}

{% block title %}ClassementEquipes index{% endblock %}

{% block body %}
 <div class="page-body">
    <h1>ClassementEquipes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ClassementEId</th>
                <th>SaisonYear</th>
                <th>EquipesEquipeId</th>
                <th>PointsTotal</th>
                <th>Position</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for classement_equipe in classement_equipes %}
            <tr>
                <td>{{ classement_equipe.id }}</td>
                 <td>{{ classement_equipe.saisonsYear.year}}</td>
                <td>{{ classement_equipe.equipesEquipeId }}</td>
                <td>{{ classement_equipe.pointsTotal }}</td>
                <td>{{ classement_equipe.position }}</td>
                <td>
                    <a href="{{ path('app_classement_equipes_show', {'classementEId': classement_equipe.id}) }}">show</a>
                    <a href="{{ path('app_classement_equipes_edit', {'classementEId': classement_equipe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_classement_equipes_new') }}">Create new</a>
     </d>
{% endblock %}
