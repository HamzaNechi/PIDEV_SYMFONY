{% extends 'base-back.html.twig' %}

{% block title %}Ajouter un utilisateur{% endblock %}


{% block body %}
<div class="page-body">
          <div class="container-fluid">
            <div class="page-header">
              <div class="row">
                <div class="col-sm-6">
                  <h3>Ajouter compte</h3>
                  <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('admin')}}"><i data-feather="home"></i></a></li>
                    <li class="breadcrumb-item">Ajouter compte</li>
                  </ol>
                </div>
                
              </div>
            </div>
          </div>
          <!-- Container-fluid starts-->
          <div class="container-fluid">
            <div class="row">
            <div class="col-sm-12 col-xl-2"></div>
              <div class="col-sm-12 col-xl-8">
                <div class="row">
                  <div class="col-sm-12">
                    <div class="card">
                      <div class="card-header pb-0">
                        <h5>Ajouter compte</h5><span>Using the <a href="#">card</a> component, you can extend the default collapse behavior to create an accordion.</span>
                      </div>
                      <div class="card-body">
                        {{ form_start(form,{'attr' :{'novalidate':'novalidate'}}) }}
                          <div class="mb-3">
                            <label class="col-form-label pt-0" for="exampleInputEmail1">Name</label>
                            {{ form_widget(form.name) }}
                             {{ form_errors(form.name)}}
                          </div>

                          <div class="mb-3">
                            <label class="col-form-label pt-0" for="exampleInputEmail1">Email</label>
                            {{ form_widget(form.email) }}
                             {{ form_errors(form.email)}}
                          </div>


                          <div class="mb-3">
                            <label class="col-form-label pt-0" for="exampleInputEmail1">Password</label>
                            {{ form_widget(form.password) }}
                             {{ form_errors(form.password)}}
                          </div>


                          <div class="mb-3">
                            <label class="col-form-label pt-0" for="exampleInputEmail1">N° Telephone</label>
                            {{ form_widget(form.tel) }}
                             {{ form_errors(form.tel)}}
                          </div>

                          <div class="mb-3">
                            <label class="col-form-label pt-0" for="exampleInputEmail1">Roles</label>
                            {{ form_widget(form.roles) }}
                             {{ form_errors(form.roles)}}
                          </div>

                          <div class="mb-3">
                            <label class="col-form-label pt-0" for="exampleInputEmail1">Photo de profil</label>
                            {{ form_widget(form.imagename) }}
                             {{ form_errors(form.imagename)}}
                          </div>
                          
                        {{ form_end(form) }}
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              
            </div>
          </div>
          <!-- Container-fluid Ends-->
        </div>


{% endblock %}

                                   